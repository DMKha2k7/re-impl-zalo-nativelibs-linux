{
  "name": "zimage",
  "version": "1.0.0",
  "description": "Zalo zimage native addon – libvips thumbnail for Linux",
  "main": "index.js",
  "scripts": {
    "build": "node-gyp rebuild && node scripts/postbuild.js",
    "test": "node test.js"
  },
  "dependencies": {
    "node-addon-api": "^8.3.1"
  }
}
